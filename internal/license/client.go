package license

import "errors"

// fetchCredentials contacts the license server and returns BuilderCredentials.
// Full implementation is done in Task 4.
func fetchCredentials(serverURL, token, version string) (*BuilderCredentials, error) {
	return nil, errors.New("license server fetch not yet implemented")
}
