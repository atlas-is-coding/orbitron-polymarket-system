package updater

import "context"

// RunUpdate downloads and applies the binary update.
// This stub will be replaced in Task 8.
func RunUpdate(ctx context.Context, version, binaryURL, dir string, p *Pending, n *Notifier) {
}
